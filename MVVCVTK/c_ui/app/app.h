#pragma once
#include <memory>

namespace app {
    class App
    {
    public:
        App();
        int run(int argc, char** argv);
    private:
        void applyGlobalStyle() const;//≈‰÷√
    };
} // namespace app